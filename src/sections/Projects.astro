---
import ProjectCard from "../components/ProjectCard.astro"
import DevIcon from "../components/icons/DevIcon.astro"

import { getI18N } from "../i18n";

const { currentLocale = "es" } = Astro;
const i18n = getI18N({ currentLocale });
---
<div id="projects"></div>

<section class="w-screen flex flex-col gap-16 dark:text-CS-Dark-Font text-CS-Light-Font">

    <div class="flex flex-col gap-3 items-center">
        <h2 class="font-semibold text-4xl">{i18n.PROJECTS.TITLE}</h2>
        <p class="text-sm text-center w-72 md:w-1/3">{i18n.PROJECTS.DESCRIPTION}</p>
        <DevIcon></DevIcon>
    </div>

    <div class="w-full flex flex-col items-center gap-10 justify-center md:flex-row">
        <ProjectCard></ProjectCard>
    </div>
</section>